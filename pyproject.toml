[project]
name = "mats12"
version = "0.1.0"
description = "MATS 12 application task workspace (Martin Herje). Research work is Martin's own; scaffolding is generic infrastructure."
requires-python = ">=3.11"
dependencies = [
    "openai>=1.40",
    "python-dotenv>=1.0",
    "pandas>=2.0",
    "matplotlib>=3.8",
    "tqdm>=4.66",
]
